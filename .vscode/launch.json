{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Viewer with MSVC",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/msvc/debug/src/Debug/viewer.exe",  // Path to your executable
            "args": [],  
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "integratedTerminal",
            "preLaunchTask": "CMake: Build with Preset" 
        }
    ]
}
